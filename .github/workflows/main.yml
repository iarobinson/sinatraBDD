name: sinatraBDD Cucumber Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        bundler-cache: true
    - run: bundle exec cucumber
